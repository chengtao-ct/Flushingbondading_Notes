
**大体上重新复盘了一下thread，object和schedule模块，现在看的差不多懂得比较多了，能看明白一点了，去看IPC模块了，我后面打算整理出一个模块是那种比较重要的点得文件夹，以后就不需要一直重复看了，去看IPC**
-  初始化流程都是先完成必要的内存配置，无论是走不走MMU内存管理单元
- 然后用自动初始化机制完成必要的遍历流程，因为必须先明确好MMU单元，才能分配内存给要初始化的模块
- 调试控制台的确定
- rt_componets_board_init 和rt_components_init是不同的，而且他们的位置都是在初始化函数里面的，rt_components__init在main_thread_entry,rt__components_board_init在rt_board_hw_init
- 只有进入第一个系统线程的时候，说明调度器完成的时候，才可以初始化高级模块
- $Sub$$main    : 编译器插入的"替身"函数的理解
- 我看了一下好多内容重复了，所以我又稍微调整了一下内容
- 数组+链表太经典了
- Null对象有什么用吗
- 对象容器和对象的结构体的思考：容器是一个大整体里面是大数组，包含了每个对象的基础信息，用rt_object_information主要是这个对象容器的大小，和链表头，每一次进行对象查找的时候，先会在容器里查找，然后比较type，返回链表头，然后再遍历链表找到需要的那个实体化结构体
- ((type *)((char *)(node) - (unsigned long)(&((type *)0)->member)))，你可以思考一下这个函数的操作
- 一个thread完整的初始化流程：先是RT_ASSERT的确认，再初始化上层对象，然后便要栈性质的初始化了，先是明确的导入参数，调度器层面的参数初始化，栈性质层面的初始化，通讯层面的初始化，定时器层面的初始化，这个关于线程得初始化我认为在看完所有以后再看会更加深刻
- 一个完整的删除流程都是从通信模块到调度器模块最后到硬件内存模块，也分为多层架构，里面涉及到了标志位的重置，线程状态的改变，资源的释放，定时器的改变
- 而且我发现了就比如rt_thread_detach行为是由两个函数实现的，自己本身只做参数层的校验，由thread_detach实现实现调度器，链表改变，IPC模块，移植到空闲模块的统一规划，我认为这一点非常值得学习
- 动态的实现需要申请两块地址，一块是动态对象的线程，也是上层模块，负责容器那边，还有一块是实际运行的大小，然后调用静态的初始化过程，delete也是一样的
- 如果一个线程的任务提前做完了，不需要傻等时间片耗尽，主动调用 `yield` 可以大幅提高系统的实时性和响应度
- 你妈的，到底在RT-Thread里面封装了多少块RT_的宏啊
- 对于线程里面的一些函数想要理解的更加深入的话，我认为还是要想看schedule层面和通信方面
- 关于挂起和唤醒的机制还是后面还需要重新研究，里面涉及到了IPC得通信
- 我去看他的Docunment文档好像没有schedule的解释，好奇怪
- 其实感觉调度的数据结构，和面对对象的数据结构非常相似，都是数组+链表，可能链表是不同的吧
- 对于调度器这个模块，特别是schedule_up模块有特别多的，与硬件相关联的调度层的上下文切换，临界区的关中断都要关，很容易接触到rt_hw的模块，有点像是对底层的封装，还有一个模块就是调度器里面的内部逻辑，数组+链表，为了实现队列的调度
- rt_hw_context_switch_to实现的方法，就是很多硬件层的作用性质认知还是不清晰，理解硬件的底层逻辑才是理解调度的本质最需要的
- 线程的调度锁本质上还是要反映到调度层面，你懂吗？因为本质上线程的切换是由调度器来完成的，其实这也让我想起了我的一个模块化的代码，我感觉写完两个项目之后要好好的去看一下，我现在感觉它一堆的问题和可以优化的地方
- 我来大体描述一下shedule的调度过程，先关中断，然后评估有没有锁，获取最高优先级，和当前的线程进行对比，分为几种模式，三种情况，优先级高，优先级一样，优先级低，然后根据情况进行调度，如果是优先级低，进行线程的切换，注意他的逻辑，就是当前线程的退出有几种逻辑，抢占和主动让出，就是里面还涉及到了Yield和sleep的特殊逻辑段，还有一个就是切换的逻辑段，就是中断的切换和普通的切换的逻辑是不一样的
- 关于底层的调度其实分为两个逻辑块，一开始是负责thread在schedule的初始化，线程的挂载啊，线程的优先级啊时间片的必要参数，还有一个负责核心的调度逻辑。主要就是优先级得存放位置，他都是用位图来完成得，切分为两种情况，看你优先级得初始化个数，如过大于32，将会采用组+位图的操作，先进行组号的明确，然后进行组里面的优先级数组确认
- 所以在看完schedule的模块之后再去看thread的实现会更加的清晰明确，因为主要就是初始化调度层的信息，包括优先级，时间片，链表位置等
- 对于线程的插入作为底层函数，只可以被上层调用，你认为有几种可能被调用呢？thread_startup,thread_yield,thread_resume等吧，主要的逻辑就是考虑现在的线程情况，位图情况，调用者情况，和再底层的链表的操作底层有关，rt_shced_thread_remove同样的逻辑，时刻要考虑位图的逻辑，因为线程的调度是靠两个数据结构共同完成的
-  线程的自旋锁啊，通信层面的限制，最终要体现在调度器层面，这也是为什么要shedule_enter_critical,因为线程的切换是全局的，你懂吗，涉及到通信和资源的释放和获取，只有上层可以调度
- 对于线程这个最为根本的模块，融合了太多的其他的模块了，还有还多模块我没有看，所以我一直对Thread模块理解不够清晰，所以说假如以后你要看第二遍Rt-thread源码，我非常不推荐模块化的看，而是代码段的实现看对吗？就是实现一个功能模块，从头到尾解析它，从最高层的封装版的API,到最底层的硬件层
- 很多最高层的API，更多的倾向于标志位，你懂吗？行为和状态是分层的，我认为分层架构的设计必然要注意这一个点，因为很多时候模块的融合调度，是必要的分层操作
- 其实我看了一下schedule_common模块，无论是thread模块或者是schedule_up或者多核的模块很多调度层面的都封装在了，common层，当然thread的初始化和删除啊什么还是调用在thread层，但是一旦到时间片和优先级层和调度层都会被上层封装，我认为为什么要上层封装，因为涉及到了模块的融合你懂吗？我感觉很多调度层会涉及到多个模块，这只是我的拙见
### 主要知识点

- 多核自旋锁的设计
- 自动初始化机制
- **面向切面编程 AOP 的 C 语言变体**
- 启动流程的顺序
- 多核下的处理
- RT_等的运用，一些常见宏的运用
- 空闲线程的运用
- 可配配置性的实现
- 对象系统的实现本质也是C里面的继承和多态的实现吧
- 多个内核对象的理解
- **链表节点塞进数据里面（链表包着数据）
- 钩子函数的运用，分为空闲钩子，线程切换钩子，内存分布钩子等
- 自己的命名规范
- 寄存器的SP指针和代码段的SP指针
- Thread如何完成多层对接的，从调度器的上下文到寄存器的上下文保存和对接
- 对于静态创建和动态创建的理解，从系统架构方面到物理内存方面，全方位思考
- 你可以思考一下，如何一直把CPU的调度一直给schedule里面的本质的调度中心：```c
rt_cpu_self()->current_thread = to_thread;
- 上下文的切换分为的内型，这让我想起来了一个问题：就是Rt-thread支持的指令集有哪些，还是说它只是作为操作系统不影响其的芯片架构
- 关于256个优先的实现的方法
- 注意内核私有函数和用户可以调用的函数
- 关于RT-thread的掩码操作你也可以看一下，因为一般的掩码操作我也知道了
- 对于抢占型调度器一个很根本的注意事项就是时间Timer模块
-
