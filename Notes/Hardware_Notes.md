## 硬件内存的架构
![500]({FF82B8F5-2CB8-454F-882F-423F0D9E7AA3}.png)

数据流动的方向：
CPU 
↔
↔
 缓存 
↔
↔
 内存 
↔
↔
 硬盘
(数据必须一级一级上下传，通常不能越级)
## 缓存，内存，硬盘的区别
1. 缓存是SRAM集成于CPU内部，速度非常快，使用的是锁存器，断电无效
2. 内存是DRAM就是俗称的内存条，速度较快用的是电容判断，断电无效，详细看[How does Computer Memory Work?](https://www.youtube.com/watch?v=7J7X7aZvMXQ)
3. 硬盘是现在基本上是SSD,由PCB,控制芯片，FLASH，可能还有DRAM，断电有效，详细看[How do SSDs Work?](https://www.youtube.com/watch?v=5Mh3o886qpg)

## SRAM的芯片基本操作（stm32）：IS62WV51216ALL,IS62WV51216BLL
1. "基本构造"
![500]({C075F962-12A9-462A-9299-2852C4C00D23}.png )

***

2. "各引脚操作"
![基本信息]({8413A36A-9A7D-4D36-8A6A-72F5B8D3E006}.png )

***

3. "读操作"
![读操作电平]({32CF7AB0-3C82-453D-99B2-82B0EB64A453}.png )




## 虚拟内存
1. 虚拟内存是谎言，他会映射到物理内存（RAM）和硬盘里面，所以他有扩张性质但因为映射到硬盘里的数据要提取出来，要经过RAM,速度会变慢（涉及swap区）
2. 虚拟内存在操作系统里面的作用：内存保护(防止了程序的内存越界访问，当然还有其它操作)，内存扩展，内存共享(对于相同的代码段，可以映射到同一块物理内存)
3. MMU实现了虚拟内存到物理内存的转化



## FSMC/FMC（内存扩展）的实现

本质：硬化的有限状态机（FSM），固化时频来模拟“地址建立-写使能低电平-数据保持-写使能高电平”这一系列物理过程

主要用在这几个方向：sRAM,FLASH,LCD，DRAM（动态的FMC）
![alt text]({09314EBF-0ADC-4056-9B0A-F1372721A241}.png)
1. 用时钟信号去模拟储存器协议，类似于通讯协议，但比普通的通讯协议快很多，并且能随意调用内存，一个多线并行的原因，还有就是内存映射的原因
2. 线数非常多：数据线，地址线，片选线，写使能线，读使能线,其他控制线
3. 内存映射：就是MCU一开始就会分配好内存地址，这也是为什么可以当作第二sRAM的原因
4. 在STM32里面的一些配置：主要是调控时许来符合储存器所需要求，还有就是一些中断啊，使能信号

![alt text]({EA637D77-A7B7-44EE-B708-9AF5C6200FD5}.png)

  
**一些提问**：
1. 用GPIO模拟引脚反转的不好的地方：一个抖动，还有就是CPU 的指令流水线。即使主频很高，CPU 翻转一个 GPIO 需要“读取指令 -> 解码 -> 写寄存器”多个周期
2. 总线冲突 ：用时初始化两个CS，导致数据传输冲突，毁坏MCU
3. 168MHz 的一个时钟周期（HCLK）大约是多少纳秒 (ns): 1/168,000,000≈5.95×10 −9秒，即 5.95 纳秒 (ns)
4. DataSetupTime 压得太死（比如只留了 1 个 HCLK 周期）：在高温下数据还没稳定（Settle）到逻辑高电平，LCD 就开始采样了。反映在屏幕上就是**“随机噪点（雪花）”、“颜色位移”或“初始化失败导致白屏”

## 显示屏(主要是在单片机里面的)
1. 无论是LED,LCD,OLED都要这三个基本参数：分辨率，尺寸大小，色彩深度。显存大小 (Frame Buffer Size) = 水平分辨率 × 垂直分辨率 × (色彩深度 / 8)。这也说明了显示一张图片的巨大信息量，这时一般都有一个显存（一般在显卡里面），在单片机里面一般的液晶显示器都会配备一个液晶控制器。
2. 在一个LCD里面存在着液晶控制器，触摸控制器
3. 液晶控制器： 主要是引脚配置，命令表，通讯方式，时序图的看懂吧 
4. 一些基本流程(ILI9806G芯片)
5. 刷新率的计算要会

来源于Genimi

---
（LCD）的核心技术。
为了让你在未来的开发中具备从底层向上层抽象的能力，我将今天所有的知识点按照底层物理、逻辑映射、库函数配置、系统权衡四个维度为你总结如下：
一、 底层物理：FSMC 模拟 8080 时序的本质
驱动 LCD 的核心在于 STM32 的 FSMC（灵活静态存储控制器） 外设。
1. 协议转换：LCD 通常采用 8080 并行接口，包含 16 位数据线、片选（CS）、读使能（RD）、写使能（WR）和命令/数据选择（RS）。
2. 硬化状态机：FSMC 本质上是一个硬件有限状态机。它将复杂的引脚翻转过程（如先拉低写使能、放数据、再拉高写使能）固化在电路中。
3. 效率对比：相比于软件模拟（Bit-Banging），FSMC 的硬件自动化能极大地提升传输速度，并消除因指令周期带来的时钟抖动。
二、 逻辑映射：“地址即控制”的神来之笔
这是你今天理解最深刻，也最需要掌握的工程技巧：
1. RS 引脚的映射：野火将 LCD 的 RS（命令/数据选择） 引脚连接到了 FSMC 的 A16 地址线上。
2. 地址偏移逻辑：
    ◦ 由于 F407 在 16 位模式下存在内部地址右移一位的物理特性，为了让外部 A16 产生电平变化，程序内部需要操作第 17 位。
    ◦ 命令地址：0x6C000000（A16 为低电平，LCD 视为命令）。
    ◦ 数据地址：0x6C000000 + (1 << 17) = 0x6C020000（A16 为高电平，LCD 视为数据）。
3. 软件抽象：通过定义两个结构体指针，你可以像读写内存 SRAM 一样操作 LCD，彻底告别了频繁操作 GPIO 的繁琐。
三、 标准库配置：微秒级的时序调优
在调用标准库初始化 FSMC 时，核心在于两个结构体：
1. FSMC_NORSRAMTimingInitTypeDef（时序参数）：
    ◦ AddressSetupTime（地址建立时间）：确保地址信号稳定。
    ◦ DataSetupTime（数据建立时间）：确保 LCD 采样时数据已在总线上稳定。
    ◦ 单位：这些参数均以 HCLK 周期为单位（F407 主频 168MHz 时，1 个周期约为 6ns）。
2. FSMC_NORSRAMInitTypeDef（模式配置）：
    ◦ 将内存类型设为 FSMC_MemoryType_SRAM。
    ◦ 选择对应的 Bank（野火使用 Bank1 Subbank4）。
四、 系统权衡与代价（工程师的进阶思维）
我们基于“没有免费的午餐”公理，分析了高性能显示背后的代价：
1. 显示瓶颈：对于 800x480 的 16 位色屏幕，维持 60Hz 刷新率需要约 44MB/s 的带宽，这已接近 FSMC 在模拟 8080 时序下的物理极限。
2. 总线竞争：
    ◦ 使用 DMA 可以释放 CPU 算力。
    ◦ 代价：DMA 持续占据 FSMC 总线会产生“总线竞争”，导致 CPU 访问挂在同一总线上的外部 SRAM 时被迫挂起，增加系统的实时抖动。
3. 边界故障：
    ◦ 信号完整性：时序压得太死会导致在极端温度下出现花屏（采样违例）。
    ◦ 状态 mismatch：LCD RESET 信号受干扰会导致其内部配置丢失，此时仅重启 FSMC 无效，必须重新执行完整的 LCD 初始化序列。
---
![alt text]({547CEDF1-BBC1-4F1B-90BB-A0E4BA8E11A7}.png)

5. 与LCD的交互通讯协议： SPI,8080并行(野火就是用FMSC模拟了这个时序，因为他和Nor Flash类似，地址线转换成，命令和数据辨别线DX)，6800并行. 

6. RS与地址总线的转化： 这种“地址即控制”的转化是由硬件电路瞬间完成的，不存在软件开销

## DMA(直接存储器访问)

DMA的本质作用就是为了解放cpu，让外设与内存之间建立起一个沟通通道，实现计算与iO并行

*分为两部分：一个是在完整的操作系统下的操作，一个是在单片机的操作*

为什么划分这两者：第一个DMA是不同的，总线架构是不同的，他们的仲裁方式与cpu的合作方式也是不同的

操作系统：

[Bitlemon: DMA Controller: How Peripheral Devices Transfer Data to RAM](https://www.youtube.com/watch?v=s8RGHggL7ws&list=PL38NNHQLqJqZoDp4CrAueD1aBin7OebEL&index=3)

电脑端的DMA特点：

1. 高度抽象：不需选通道，流啊什么的(涉及到硬件)，直接调用操作系统内核与设备驱动程序写好的API
2. 性能之上：电脑中的DMA控制器（通常集成在芯片组里）非常强大，支持极高的带宽和复杂的“分散-聚集 (Scatter-Gather)”操作（可以一次性把内存中不连续的多个数据块，合并发送出去）

单片机端(stm32)的特点：

来源于Genimi

![alt text]({05FFF5AF-2A85-4127-AF01-3241C0AF101E}.png)

来源于Genimi的幼稚园比喻：

![alt text]({944E6EBD-1B46-4D06-A81C-3BFF1B7053EB}.png)

1. 硬件结构：

![]({3812064B-6DAE-4239-8CA1-DC16BB8CE03E}.png)

2. 通道与流：8*8结构，一个流里面只可以有一个通道在使用，但可以多个流共同操作，通道与流涉及到DMA请求映射

3. 仲裁器： 由编程端端口来软件控制和数据流编号小的优先级大

4. FIFO(优化传输): 

两大优化点：
1. 批处理 (Batching): 通过突发传输和阈值触发，将多次零散的总线访问合并为少数几次批量的总线访问，减少了总线仲裁的次数，提高了数据吞吐率。
2. 解耦合 (Decoupling): 充当高速源和低速目标之间的缓冲区，允许DMA快速完成对高速总线的访问并立即释放，从而降低了总线占用时间，提升了整个系统的并行处理能力和实时性。

基本配置：

![alt text]({679D28B0-BAB9-4E73-8E16-180EAA7E8145}.png)

突发： 理解为一次取数据（包裹）到 FIFO(快递站)

阈值： 理解为当FIFO(快递站)满了多少阈值包裹就占用AHB总线发送到目的地

5. 要配置的一些数据

来源于Genimi

![alt text]({3A22015A-4E3A-4937-86A1-4A7FB233886E}.png)


6. 工程性问题(DMA (直接存储器访问) 与 Cache 一致性和其受到的内存对齐问题):



## CPU与MCU的区别：

主要在于集成度不一样：一个是精细化大脑，一个大脑萎缩的人

来源于Genimi

![alt text]({B414FD85-EEB9-4A5F-8740-F05824CE0D77}.png)

## 51单片机(8051)->stm320>树莓派(BCM2711 on Raspberry Pi 4B)

![alt text]({8982C883-6083-496F-A124-F5C1D3646131}.png)

## 总线的基本架构：单一总线->AMBA多总线矩阵->更复杂的AXI/ACE总线

主要介绍STM32的基本架构:

![alt text]({9268D7F3-FD2D-4DA3-A552-5F556ADEEDB2}.png)

AHB:主要挂载需要高速的东西(时钟频率高)：CPU内核内存 (Flash, SRAM)，DMA控制器 (数据搬运工必须走快速路！)，GPIO (为了实现快速翻转)

APB:经过AHB分频了的线路，适配那些普通的部门：can,spi，i2c，adc等

优势：

![alt text]({633061F0-91FB-4DA3-80B5-855351B886EE}.png)















