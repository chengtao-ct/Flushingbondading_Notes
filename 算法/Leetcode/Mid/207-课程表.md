---
aliases: [Course Schedule, 拓扑排序, Kahn算法]
tags:
  - 算法
  - Leetcode
  - 图
  - 拓扑排序
difficulty: Mid
leetcode: 207
related:
  - "[[198-打家劫舍]]"
date: 2026-04-14
status: ✅已完成
---

> [!abstract] 题目概述
> **考察方向：** Kahn 算法（拓扑排序）。
>
> **嵌入式场景：** 假设你正在为一个复杂的工业主板写 Bootloader（启动代码）。板子上有 10 个外设需要初始化。但是，I2C 必须在 DMA 之后启动，DMA 必须在时钟（Clock）之后启动，而时钟又依赖电源管理芯片。你如何写一个算法，给出一个**绝对安全的启动顺序**？更致命的是，如果硬件工程师画错了图，导致 A 依赖 B，B 依赖 A（死锁环），你的算法如何第一时间报警？
>
> **挑战点：** 这是你第一次真正接触"图"结构。如何用数组把错综复杂的依赖关系表示出来（邻接表）？如何像剥洋葱一样，从"没有任何依赖"的节点一层层往里剥？

> [!note] 思考过程
> 问题：图这个结构，是不是在Rt-thread的优先级调度里面存在的啊，我记得它是用数组+双向链表完成一些任务的？依赖关系没有一点思路啊？一开始对多维数组理解不够充分？
>
> 思路：其实他的大体思路是先看到这个，要思考出来用什么来解决，这里用的拓扑排序。然后但是这个题目的形成完全不是拓扑算法，所以说你要架构成拓扑算法——形成入度表和邻接表，最后才是调度过程。

> [!warning] 踩坑记录
> 一开始对多维数组理解不够充分。题目本身不是拓扑算法的形式，需要自己架构成拓扑算法。

## 解法

```c
bool canFinish(int numCourses, int** prerequisites, int prerequisitesSize, int* prerequisitesColSize) {

    int inDegree[2000] = {0};
    int adj[2000][2000] = {0};
    int adjCount[2000] = {0};

    for (int i = 0; i < prerequisitesSize; i++) {
        int target = prerequisites[i][0];
        int pre    = prerequisites[i][1];

        inDegree[target]++;
        adj[pre][adjCount[pre]++] = target;
    }

    int queue[2000];
    int head = 0, tail = 0;

    for (int i = 0; i < numCourses; i++) {
        if (inDegree[i] == 0) {
            queue[tail++] = i;
        }
    }

    int completedCount = 0;

    while (head < tail) {
        int current = queue[head++];
        completedCount++;

        for (int i = 0; i < adjCount[current]; i++) {
            int nextCourse = adj[current][i];
            inDegree[nextCourse]--;

            if (inDegree[nextCourse] == 0) {
                queue[tail++] = nextCourse;
            }
        }
    }

    return completedCount == numCourses;
}
```

## 原理剖析

要想用 C 语言实现这个过程，你需要准备三样东西：

1. **入度账本（数组）：** `int inDegree[numCourses] = {0};` 用来记录每个任务当前被几个条件卡着。
2. **小本本（邻接表）：** 因为当你执行完任务 C 时，你需要通知 B 和 D（它们的入度减 1）。所以你需要记录**"我执行完后，该去通知谁"**。
3. **就绪队列（数组模拟）：** `int queue[numCourses];` 把所有入度为 0 的任务扔进去，然后排队一个个执行。

### 入度表 (In-Degree Array) —— "我的紧箍咒"

- **本质：** 它是一个**向内看**的数据结构。
- **它回答的问题：** "我现在能马上开始干活吗？"
- **怎么用：** 员工 B 每天早上来看一眼自己的入度表 `inDegree[B]`。如果上面的数字是 2，代表他头上还有两个领导（前置任务）没发话，他只能干等（不能进队列）。他**根本不关心**是哪两个领导卡着他，他只关心"数字什么时候变成 0"。只要数字是 0，他立刻冲进"就绪队列"开始干活！

### 邻接表 (Adjacency List) —— "我的通知名单"

- **本质：** 它是一个**向外看**的数据结构。
- **它回答的问题：** "我干完活了，我该去通知谁？"
- **怎么用：** 员工 A 终于把活干完了。他立刻翻开自己的小本本 `adj[A]`，发现上面写着 `[B, C]`。他瞬间明白："我要去解开 B 和 C 头上的紧箍咒！"他**根本不关心** B 和 C 还有没有被别人卡着，他只负责去告诉他们："我这关你过了"。

> [!tip] 核心收获
> 拓扑排序的核心是两个视角：入度表向内看（"我能开工吗？"），邻接表向外看（"我干完了该通知谁？"）。最终判定：如果完成数 == 总课程数，无死锁；否则存在循环依赖。大体思路：先识别算法类型 → 架构数据结构（入度表 + 邻接表）→ 执行调度。
