`Phase 2 不追求真云，不追求边缘 AI，不追求复杂 UI。先做一个局域网 PC AI 服务端，把摄像头画面变成人数，把人数稳定变成 5 路灯。`



对，完全可以理解成两个工作面：

**1. ESP32 端代码修改**

ESP32 负责“嵌入式主控”这部分：

- 从 USB UVC 摄像头拿到 latest JPEG
- 通过 Wi-Fi 把 JPEG 发给 PC 服务端
- 调用类似 `POST /api/detect`
- 解析服务端返回的 JSON，例如 `person_count`
- 根据人数执行 `light_policy`
- 通过 CDC 给 CH552G 下发 `L1 ON/OFF` 到 `L5 ON/OFF`
- 做断网、HTTP 失败、JSON 解析失败时的降级处理

也就是说，ESP32 不跑 AI，它只负责采图、上传、解析结果、决策和灯控。

**2. 本地局域网 PC 部署**

PC 负责“云端 AI 服务”这部分：

- 开一个 FastAPI / Flask 服务
- 监听局域网地址，比如 `http://电脑IP:8000`
- 提供 `/api/health` 和 `/api/detect`
- 启动时加载 YOLOv8n 模型
- 收到 ESP32 上传的 JPEG
- 运行人员检测
- 返回 JSON：


**第 1 阶段：PC 云端服务骨架**

目标不是马上 AI，而是先做一个局域网 HTTP 服务。

PC 服务提供两个接口：

```
GET  /api/health
POST /api/detect
```

`/api/health` 用来确认服务活着。

`/api/detect` 先不跑 YOLO，只返回固定人数，例如：

```
{
  "status": "ok",
  "person_count": 1,
  "latency_ms": 3,
  "model": "fake-detector-v1"
}
```

这一步成功的标准是：你用浏览器或 PowerShell 访问 PC 服务，能看到 JSON 返回。

**第 2 阶段：让 ESP32 发到 `/api/detect`**

你现在 ESP32 已经能把 JPEG 发到 `/api/upload`。下一步不是重写网络层，而是把目标接口换成：

```
http://你的电脑IP:8000/api/detect
```

这一步 ESP32 还不用关心 YOLO。它只需要确认：

```
ESP32 -> POST JPEG -> PC
PC -> 返回 person_count
```

成功标准是 PC 终端能打印“收到一张 JPEG”，ESP32 串口能看到 HTTP 200。

**第 3 阶段：ESP32 解析 JSON**

这一步才开始让 ESP32 读返回内容。

ESP32 要从响应里解析：

```
status
person_count
latency_ms
```

然后串口打印：

```
detect result: person_count=1 latency=3ms
```

成功标准是：PC 返回固定 `person_count=1`，ESP32 能稳定解析出来。

**第 4 阶段：接入灯控策略**

假检测结果能解析后，再接 `light_policy`。

例如：

```
person_count=0 -> 全灭
person_count=1 -> L1 ON
person_count=2 -> L1,L2 ON
person_count>=5 -> L1~L5 ON
```

这一步还是不接 YOLO。因为我们先验证“人数结果 -> LED”这条链路。

**第 5 阶段：PC 服务接入 YOLO**

前四步都稳定之后，才把 PC 的 fake detector 换成 YOLOv8n。

也就是：

```
原来：
JPEG -> fake person_count

改成：
JPEG -> YOLOv8n -> person_count
```

这样如果出问题，你马上知道问题在 AI 模型，而不是 HTTP、JSON、ESP32、灯控混在一起。

所以我们的教学顺序是：

```
PC 服务活着
-> PC 假检测
-> ESP32 能 POST
-> ESP32 能解析 JSON
-> LED 能按人数变化
-> 最后接 YOLO
```


你现在直接运行这个：

```
curl.exe -X POST "http://127.0.0.1:8000/api/detect" `
  -H "Content-Type: image/jpeg" `
  --data-binary "@C:\Users\tao93\Desktop\5.26Project\ESP32S3_USB_Host\uploads\latest.jpg"
```

或者先进入项目根目录：

```
cd C:\Users\tao93\Desktop\5.26Project
```

然后运行：

```
curl.exe -X POST "http://127.0.0.1:8000/api/detect" `
  -H "Content-Type: image/jpeg" `
  --data-binary "@ESP32S3_USB_Host\uploads\latest.jpg"
```

成功后应该返回类似：

```
{
  "status": "ok",
  "person_count": 1,
  "latency_ms": 0,
  "image_len": 8897,
  "jpeg_soi": true,
  "jpeg_eoi": true,
  "model": "fake-detector-v1"
}
```

记住这个嵌入式联调知识点：`curl -`


我先做了项目体检：你这个工程已经不是“刚拼起来”，而是进入 Phase 2 融合早期了。

当前结构大致是：

- `ESP32S3_USB_Host`：ESP32-S3 做 USB Host，接 UVC 摄像头 + CH552G CDC 灯控。
- `PC_AI_Server`：FastAPI 接收 JPEG，返回 `person_count`。
- `light_policy`：已经把人数映射成 1~5 路 LED，并带 3 秒无人滞回。
- 测试结果：ESP32 源码级测试 23 个通过，PC 服务端测试 10 个通过。
- 明显待补强点：容错降级、Wi-Fi 断线恢复、HTTP/JSON 契约更稳、版本管理忽略 build/缓存产物、PC 推理阻塞事件循环这一类融合阶段问题。

我先问你一个关键问题：你接下来最想优先冲哪一个目标？

1. **先跑通演示闭环**：摄像头看到人，PC 识别人数，LED 跟随变化。
2. **补强稳定性**：断网、AI 服务挂掉、CDC 失败时系统不崩，有降级策略。
3. **做成答辩/简历项目**：补验收数据、架构图、对比表、测试记录，让项目讲得漂亮。