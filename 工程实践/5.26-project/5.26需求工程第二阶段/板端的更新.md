- Phase 1 已经跑通：`Camera -> latest JPEG cache -> HTTP upload -> mock detect -> CH552G CDC -> L1 ON/OFF`
- 当前 ESP32 主线在 `APP_STAGE_INTEGRATION`
- `camera_uvc_client`、`ch552_cdc_client`、`usb_host_install`、`board_power` 是稳定地基，原则上不动
- 现在缺的是 Phase 2：把 mock detect 替换为 `/api/detect` 返回的 `person_count`
- ESP32 侧需要新增/调整：
    - `cloud_http_client`：新增 detect 请求并解析 JSON
    - `light_policy`：新增人数到 L1~L5 的决策、3 秒无人滞回、changed 标志
    - `wifi_sta_client`：后续加连接状态/重连
    - `app_state.c`：把集成阶段改成真实检测链路
    - `project_config.h`：补 detect URL、滞回、降级阈值等参数



我推荐路线是 **先做最小真实检测链路**：

1. 在 `project_config.h` 新增：  
    `APP_HTTP_DETECT_URL "http://172.20.150.56:8000/api/detect"`
    
2. 在 `cloud_http_client` 新增函数：  
    `cloud_http_client_detect_person(...)`  
    它复用 latest JPEG cache，POST 到 `/api/detect`，解析 JSON 里的 `person_count` 和 `latency_ms`。
    
3. 新增 `light_policy` 模块：  
    输入 `person_count`，输出 5 路 LED 目标状态。  
    第一版先实现：  
    `0 -> 全灭`  
    `1 -> L1`  
    `2 -> L1+L2`  
    ...  
    `>=5 -> 全亮`  
    滞回 3 秒可以作为下一小步加入。
    
4. 改 `app_state.c` 的 integration 主循环：  
    去掉奇偶数 mock detect。  
    改成：  
    `latest JPEG -> /api/detect -> person_count -> light_policy -> ch552_cdc_set_led(L1~L5)`